# sps-harness v0.5.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adopt 6 Claude Code best-practice mechanisms into sps-harness to improve skill efficiency, agent memory, and configuration consistency.

**Architecture:** Pure configuration changes — editing markdown frontmatter, adding shell injection blocks, creating one settings.json, and syncing documentation. No new scripts or logic.

**Tech Stack:** Markdown (YAML frontmatter), JSON, Bash (shell injection commands)

---

### Task 1: Agent Memory — security-reviewer

**Files:**
- Modify: `agents/security-reviewer.md:1-10` (frontmatter)
- Modify: `agents/security-reviewer.md:14` (insert new section before Scan Procedure)

- [ ] **Step 1: Add `memory: project` to frontmatter**

Replace the frontmatter block in `agents/security-reviewer.md`:

```yaml
---
name: security-reviewer
memory: project
description: Read-only security audit agent. Checks OWASP Top 10, hardcoded secrets, dependency vulnerabilities, and attack surface.
model: sonnet
maxTurns: 30
allowedTools:
  - Read
  - Glob
  - Grep
---
```

Only change: added `memory: project` on line 3. All other fields unchanged.

- [ ] **Step 2: Insert Memory Usage section**

Insert the following block after line 14 (`You are a read-only security auditor...`), before `## Scan Procedure`:

```markdown

## Memory Usage

Before scanning, read your memory to check for:
- Previously confirmed false positives (skip these in the report, note them as "Known — accepted")
- Last audit findings (compare and mark each as "New", "Fixed", or "Still present")
- Known attack surface baseline (highlight changes since last audit)

After scanning, write a brief summary to memory:
- Date and scope of this audit
- New findings with severity
- Any items the user explicitly marked as false positive during this session
```

- [ ] **Step 3: Verify the file**

Read `agents/security-reviewer.md` and confirm:
1. Frontmatter has `memory: project`
2. `## Memory Usage` section exists between the intro paragraph and `## Scan Procedure`
3. Rest of the file (OWASP checklist, patterns, output format) is untouched

- [ ] **Step 4: Commit**

```bash
git add agents/security-reviewer.md
git commit -m "feat(security-reviewer): add cross-session memory with project scope"
```

---

### Task 2: context: fork + Stop Hook — verification-loop

**Files:**
- Modify: `skills/verification-loop/SKILL.md:1-4` (frontmatter)

- [ ] **Step 1: Update frontmatter**

Replace the frontmatter block in `skills/verification-loop/SKILL.md`:

```yaml
---
name: verification-loop
description: Run a 6-phase full-project verification (build, types, lint, tests, security, diff review) to check overall project health. Trigger when the user wants a broad project-wide check before merging, releasing, or creating a PR, or after completing a feature/bugfix. Trigger phrases include "verify", "run checks", "audit the project", "check everything", "pre-PR check", "harness audit", "is the project ready", "帮我检查一下项目", "全面检查", "跑通吗", "有没有问题". Also trigger when the user expresses anxiety about breaking things after a large change and wants the whole codebase validated. Skip when the user is: reviewing a specific GitHub PR, writing or adding tests, setting up or configuring linters/tools, fixing a single known bug or type error, code migration, or investigating a specific CI pipeline failure.
context: fork
hooks:
  Stop:
    - type: prompt
      prompt: >
        Before reporting results, confirm: every PASS phase has the actual
        command output quoted as evidence. No phase can be marked PASS without
        showing the real terminal output.
---
```

Changes: added `context: fork` and `hooks.Stop` block. Description unchanged.

- [ ] **Step 2: Verify the file**

Read `skills/verification-loop/SKILL.md` and confirm:
1. Frontmatter has `context: fork`
2. Frontmatter has `hooks:` → `Stop:` → `type: prompt` with the evidence-checking prompt
3. SKILL.md body (everything after `---`) is completely untouched

- [ ] **Step 3: Commit**

```bash
git add skills/verification-loop/SKILL.md
git commit -m "feat(verification-loop): add context fork and evidence-checking stop hook"
```

---

### Task 3: Dynamic Shell Injection + Description — harvest

**Files:**
- Modify: `skills/harvest/SKILL.md:1-9` (frontmatter — description rewrite)
- Modify: `skills/harvest/SKILL.md:11-12` (insert shell injection block)

- [ ] **Step 1: Rewrite description in frontmatter**

Replace the frontmatter in `skills/harvest/SKILL.md`:

```yaml
---
name: harvest
description: >
  Use when: starting a new project after brainstorming, project has no .claude/CLAUDE.md,
  or design docs changed and CLAUDE.md needs refresh.
  Trigger on: "harvest", "/harvest", "生成CLAUDE.md", "generate project config",
  "sync design docs to claude", "把设计文档同步到.claude".
  Use after Superpowers brainstorming produces a design doc, before writing-plans.
  Also use on existing projects that lack .claude/CLAUDE.md.
  Do not trigger for: editing existing CLAUDE.md content (user does that manually),
  .claude/ health assessment (harness-audit's job).
---
```

Changes: added "Use when:" prefix, added "Do not trigger for:" exclusions.

- [ ] **Step 2: Insert shell injection block**

Insert the following between `# Harvest` / `Generate .claude/CLAUDE.md...` intro line and `## Two modes`:

```markdown

## 当前项目快照

- Git 分支: !`git branch --show-current 2>/dev/null || echo "not a git repo"`
- 包管理器: !`(ls pnpm-lock.yaml 2>/dev/null && echo pnpm) || (ls yarn.lock 2>/dev/null && echo yarn) || (ls package-lock.json 2>/dev/null && echo npm) || echo "unknown"`
- Node 版本: !`node -v 2>/dev/null || echo "N/A"`
- Python 版本: !`python3 --version 2>/dev/null || echo "N/A"`
- 设计文档: !`ls docs/superpowers/specs/*.md 2>/dev/null || echo "none"`
- 现有 CLAUDE.md: !`(test -f .claude/CLAUDE.md && echo "exists, $(wc -l < .claude/CLAUDE.md) lines") || echo "not found"`

```

This block goes between the intro paragraph and `## Two modes`.

- [ ] **Step 3: Verify the file**

Read `skills/harvest/SKILL.md` and confirm:
1. Description starts with "Use when:"
2. Description includes "Do not trigger for:"
3. `## 当前项目快照` section exists with 6 `!`command`` lines
4. `## Two modes` follows immediately after the snapshot section
5. Rest of the file (modes, output structure, workflow, known issues, important) unchanged

- [ ] **Step 4: Commit**

```bash
git add skills/harvest/SKILL.md
git commit -m "feat(harvest): add dynamic shell injection and optimize description trigger"
```

---

### Task 4: Dynamic Shell Injection + Description — harness-audit

**Files:**
- Modify: `skills/harness-audit/SKILL.md:1-16` (frontmatter — description rewrite)
- Modify: `skills/harness-audit/SKILL.md:22` (insert shell injection block)

- [ ] **Step 1: Rewrite description in frontmatter**

Replace the frontmatter in `skills/harness-audit/SKILL.md`:

```yaml
---
name: harness-audit
description: >
  Use when: user wants to know how well their .claude/ is set up, starting a new project
  and unsure what .claude/ should contain, or periodic health check after many changes.
  Trigger on: "harness audit", "审计harness", "check my harness", "评估.claude成熟度",
  "/harness-audit", "harness init", "初始化harness", ".claude健康检查", "harness体检",
  "我的harness怎么样", "score my harness", "给harness打个分", "check harness health",
  "how should I structure .claude/", "harness 成熟度", "harness 打分".
  Also trigger when the user asks to evaluate, score, or assess their .claude/ configuration health.
  Do not trigger when the user wants to create, modify, fix, or reorganize .claude/ files
  (that is harness-creator's job), review individual prompt files (prompt-audit's job),
  or configure settings.json hooks (update-config's job).
---
```

Changes: added "Use when:" prefix at the start. Trigger phrases and exclusions kept from original.

- [ ] **Step 2: Insert shell injection block**

Insert the following between the intro paragraph (`A well-structured harness...`) and `## Two modes`:

```markdown

## 当前 .claude/ 快照

- .claude/ 存在: !`test -d .claude && echo "yes" || echo "no"`
- CLAUDE.md: !`(test -f .claude/CLAUDE.md && echo "$(wc -l < .claude/CLAUDE.md) lines") || (test -f CLAUDE.md && echo "root, $(wc -l < CLAUDE.md) lines") || echo "missing"`
- rules 数量: !`ls .claude/rules/*.md 2>/dev/null | wc -l`
- agents 数量: !`ls .claude/agents/*.md 2>/dev/null | wc -l`
- hooks 配置: !`test -f .claude/hooks.json && echo "exists" || (test -f hooks/hooks.json && echo "in hooks/") || echo "missing"`
- 设计文档: !`ls docs/superpowers/specs/*.md 2>/dev/null | wc -l`

```

This block goes between the intro paragraphs and `## Two modes`.

- [ ] **Step 3: Verify the file**

Read `skills/harness-audit/SKILL.md` and confirm:
1. Description starts with "Use when:"
2. `## 当前 .claude/ 快照` section exists with 6 `!`command`` lines
3. `## Two modes` follows after the snapshot section
4. All 7 dimensions, maturity levels, output format, known issues unchanged

- [ ] **Step 4: Commit**

```bash
git add skills/harness-audit/SKILL.md
git commit -m "feat(harness-audit): add dynamic shell injection and optimize description trigger"
```

---

### Task 5: Description Optimization — strategic-compact

**Files:**
- Modify: `skills/strategic-compact/SKILL.md:1-4` (frontmatter only)

- [ ] **Step 1: Rewrite description**

Replace the frontmatter in `skills/strategic-compact/SKILL.md`:

```yaml
---
name: strategic-compact
description: >
  Use when: suggest-compact hook fires, user says /save-compact or "保存上下文",
  context feels long and you notice forgetting earlier conversation details,
  or recovering after a context reset.
  Do not trigger for: routine /compact without state saving,
  or when conversation is short and no state worth preserving.
---
```

Change: restructured from narrative sentence to "Use when:" / "Do not trigger for:" format.

- [ ] **Step 2: Verify the file**

Read `skills/strategic-compact/SKILL.md` and confirm:
1. Description starts with "Use when:"
2. Description includes "Do not trigger for:"
3. Body unchanged (Strategic Compact heading, when to compact, what survives, before/after compacting, known issues, housekeeping)

- [ ] **Step 3: Commit**

```bash
git add skills/strategic-compact/SKILL.md
git commit -m "refactor(strategic-compact): optimize description as trigger format"
```

---

### Task 6: settings.json + Rules Cleanup

**Files:**
- Create: `settings.json` (plugin root)
- Modify: `rules/security-standards.md`
- Modify: `rules/git-standards.md`

- [ ] **Step 1: Create settings.json**

Create `settings.json` in the plugin root directory:

```json
{
  "permissions": {
    "deny": [
      "Bash(git * --no-verify*)",
      "Bash(git push --force *)",
      "Bash(git push -f *)",
      "Bash(rm -rf *)"
    ]
  }
}
```

- [ ] **Step 2: Update security-standards.md**

Replace the full content of `rules/security-standards.md`:

```markdown
## Security Standards

Automated checks (commit-guard hook handles, no manual verification needed):
- Hardcoded secrets → commit-guard.js auto-scans on every commit
- console.log / debugger → commit-guard.js auto-detects on every commit

Before every commit, manually verify:
- [ ] All user inputs validated and sanitized
- [ ] Database queries use parameterized statements (SQL injection prevention)
- [ ] User-generated HTML is escaped (XSS prevention)
- [ ] CSRF protection enabled on state-changing endpoints
- [ ] Authentication and authorization verified on protected routes
- [ ] Rate limiting applied to public-facing endpoints
- [ ] Error messages expose only user-friendly descriptions, omitting stack traces, file paths, and version strings

Store secrets in environment variables or a secret manager. Validate required secrets are present at application startup, because missing secrets should fail loudly rather than silently.

Rotate any secret that may have been exposed immediately, because delayed rotation increases the window of vulnerability.

When a security issue is found: stop current work immediately, run /security-review, fix all CRITICAL issues before resuming.

<!-- Added: 2026-03-30 | Source: ECC security.md adapted for Superpowers ecosystem | Reason: 建立安全实践基线 -->
```

Changes vs original: moved "No hardcoded secrets" and "console.log" out of the manual checklist into an "Automated checks" header. Removed the TODO comment at the bottom.

- [ ] **Step 3: Update git-standards.md first line**

In `rules/git-standards.md`, change the first line from:

```
Format commit messages as `<type>: <description>`, because consistent formatting makes history scannable and enables changelog generation.
```

to:

```
Format commit messages as `<type>: <description>` (auto-validated by commit-guard hook), because consistent formatting makes history scannable and enables changelog generation.
```

Only change: added `(auto-validated by commit-guard hook)`. Rest of the file unchanged.

- [ ] **Step 4: Verify all three files**

1. Read `settings.json` — confirm 4 deny rules exist
2. Read `rules/security-standards.md` — confirm "Automated checks" header, 6 manual checklist items (not 8), no TODO comment
3. Read `rules/git-standards.md` — confirm "(auto-validated by commit-guard hook)" present in first line

- [ ] **Step 5: Commit**

```bash
git add settings.json rules/security-standards.md rules/git-standards.md
git commit -m "feat: add permissions.deny settings and clean up rule-hook duplication"
```

---

### Task 7: Documentation Sync + Version Bump

**Files:**
- Modify: `rules/workflow-map.md`
- Modify: `rules/harness-method.md`
- Modify: `README.md`
- Modify: `.claude-plugin/plugin.json`

- [ ] **Step 1: Update workflow-map.md**

In `rules/workflow-map.md`, make three additions:

**a)** In section "6. Quality Assurance", after the line `- All operations → observe hook (recording)`, add:

```markdown
- 危险命令 (rm -rf, --no-verify, git push --force) → settings.json permissions.deny (权限层拦截)
```

**b)** In section "7. Security Review", replace:

```markdown
- Auth/payment/user data changes → /security-review
```

with:

```markdown
- Auth/payment/user data changes → /security-review (cross-session memory, tracks false positives)
```

**c)** In "Work Mode Routing", replace:

```markdown
- Writing code/products: /launch → brainstorming → writing-plans → SDD → verification-loop → /security-review (if needed)
```

with:

```markdown
- Writing code/products: /launch → brainstorming → writing-plans → SDD → verification-loop (fork) → /security-review (if needed)
```

- [ ] **Step 2: Update harness-method.md**

Append the following new section at the end of `rules/harness-method.md`, after the "File Placement Principles" section:

```markdown

### Skill 运行模式选择

- **默认（inline）**：Skill 在主上下文运行，用户可追问中间过程。适合诊断类（harness-audit、prompt-audit）。
- **context: fork**：Skill 在隔离子窗口运行，只返回最终结果。适合重型管道类（verification-loop）。

选择标准：用户需要追问中间细节 → inline；用户只关心最终结论 → fork。
```

- [ ] **Step 3: Update README.md**

**a)** In "What it does" section, after the "Health assessment" bullet, add:

```markdown
- **Cross-session memory** — security-reviewer remembers past findings and false positives across sessions
```

**b)** Replace the Components table:

```markdown
| Type | Count | Contents |
|---|---|---|
| Agent | 1 | security-reviewer (read-only, cross-session memory) |
| Skills | 7 | harness-audit, harvest, product-drafter, product-launcher, prompt-audit, strategic-compact, verification-loop |
| Hooks | 9 | auto-format, typecheck, quality-gate, commit-guard, block-no-verify, completion-guard, suggest-compact, observe, session-start |
| Commands | 6 | /harvest, /security-review, /harness-audit, /save-compact, /learn, /rules |
| Rules | 8 | coding-standards, testing-standards, security-standards, git-standards, workflow-map, harness-method, noise-filter, project-structure |
```

Changes: Agent row adds "cross-session memory", Skills count 5→7, Rules count 7→8.

- [ ] **Step 4: Bump version in plugin.json and marketplace.json**

In `.claude-plugin/plugin.json`, change `"version": "0.4.0"` to `"version": "0.5.0"`.

In `.claude-plugin/marketplace.json`, change `"version": "0.4.0"` to `"version": "0.5.0"` (inside the plugins array).

- [ ] **Step 5: Verify all four files**

1. Read `rules/workflow-map.md` — confirm 3 additions (permissions.deny line, memory note, fork note)
2. Read `rules/harness-method.md` — confirm new "Skill 运行模式选择" section at the end
3. Read `README.md` — confirm cross-session memory bullet, Skills=7, Rules=8
4. Read `.claude-plugin/plugin.json` — confirm version is "0.5.0"
5. Read `.claude-plugin/marketplace.json` — confirm version is "0.5.0"

- [ ] **Step 6: Commit**

```bash
git add rules/workflow-map.md rules/harness-method.md README.md .claude-plugin/plugin.json .claude-plugin/marketplace.json
git commit -m "docs: sync documentation for v0.5.0 and bump version"
```
